[project]
name = "aws-cdk-lightsail"
version = "0.1.0"
description = "CDK project (Python + uv) that provisions a Lightsail instance with a static IP"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aws-cdk-lib==2.*",
    "constructs==10.*",
]
